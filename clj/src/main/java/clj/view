package clj.view;

import java.io.File;
import java.util.Scanner;

public class View {

    private String[] messageArray;

    private void readfile(File) {
        Scanner Reader = new Scanner(File);
        int x = 0;
        while (Reader.hasNextLine()) {
            this.messageArray[x] = Reader.nextLine();
            x++;
        }
    }

    public void updateView(){
    }
}
